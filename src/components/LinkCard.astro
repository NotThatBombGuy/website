---
const { title, href, imgSrc } = Astro.props;
---
<div class="my-link">
	<div class="my-link-title">
		<h3>{title}</h3>
		<p><slot /></p>
	</div>
	<div class="my-link-image">
		<a href={href}><img class="link-image" src={imgSrc} /></a>
	</div>
</div>
